name: "Python"

on:
  push: { branches: [ "develop" ], tags: [ "*" ] }
  pull_request: { branches: [ "develop" ] }

jobs:
  fmt:
    runs-on: ubuntu-latest
    steps:
    - uses: actions/checkout@v2
    - uses: jdx/mise-action@v2
    - uses: astral-sh/setup-uv@v6
    - run: tire fmt --check

  lint:
    runs-on: ubuntu-latest
    steps:
    - uses: actions/checkout@v2
    - uses: jdx/mise-action@v2
    - uses: astral-sh/setup-uv@v6
    - run: tire lint

  test:
    name: "Python ${{ matrix.python-version }} (${{ matrix.op }})"
    runs-on: ubuntu-latest
    strategy:
      fail-fast: false
      matrix:
        python-version: ["3.10", "3.11", "3.12", "3.13", "3.14"]
        op: ["uv run pytest", "uv run mypy src"]
    steps:
    - uses: actions/checkout@v2
    - uses: jdx/mise-action@v2
    - uses: astral-sh/setup-uv@v6
      with:
        python-version: "${{ matrix.python-version }}"
    - run: ${{ matrix.op }}

  # documentation:
  #   runs-on: ubuntu-latest
  #   needs: test
  #   steps:
  #   - uses: actions/checkout@v2
  #   - uses: NiklasRosenstein/slap@gha/install/v1
  #   - run: slap install --no-venv-check --only-extras docs
  #   - run: slap run --no-venv-check docs:build
  #   - uses: JamesIves/github-pages-deploy-action@4.1.4
  #     if: github.ref == 'refs/heads/develop'
  #     with: { branch: gh-pages, folder: docs/_site, ssh-key: "${{ secrets.DEPLOY_KEY }}" }
